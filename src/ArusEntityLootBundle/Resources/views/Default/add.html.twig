<div style="display:none;" class="modal fade" id="modalLootAdd" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
                <h4 class="modal-title" id="myModalLabel">Add a loot</h4>
            </div>
            <div class="modal-body">
                {{ form_start(loot_add_form,{'attr':{'class':'form-horizontal'}}) }}
                    {{ form_row(loot_add_form._token) }}
                    {{ form_widget(loot_add_form.descr,{'attr':{'class':'form-control','placeholder':'Your findings here...'}}) }}
                {{ form_end(loot_add_form) }}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary" id="confirm-save">Save</button>
            </div>
        </div>
    </div>
</div>
